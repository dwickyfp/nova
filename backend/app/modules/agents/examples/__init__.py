"""Bundled Agent Studio examples: a semantic model and a seed script.

``nova_sales.ossie.yaml`` is a working Ossie model over the built-in
``NOVA_DEMO`` / ``NOVA_CATALOG`` sample data. ``seed_demo`` creates it plus an
agent bound to it.
"""
